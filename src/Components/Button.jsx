const Button = ({inputData}) => {
    console.log(inputData)
    return (
        <div>
            <button className="bg-blue-700 text-white px-4 py-2 rounded-md">Print / Download</button>
        </div>
    );
};

export default Button;